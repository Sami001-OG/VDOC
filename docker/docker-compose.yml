version: "3.9"

services:
  videomarker:
    build:
      context: ..
      dockerfile: docker/Dockerfile
    ports:
      - "8080:8080"
    volumes:
      - ./data:/data
      - ./videos:/videos:ro
    env_file:
      - ../.env
    command: ["videomarker", "serve", "--host", "0.0.0.0", "--port", "8080"]
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: all
              capabilities: [gpu]

  videomarker-cli:
    build:
      context: ..
      dockerfile: docker/Dockerfile
    volumes:
      - ./data:/data
      - ./videos:/videos:ro
    env_file:
      - ../.env
    entrypoint: ["videomarker"]
    profiles:
      - cli
